
* ansible-galaxy role init ec2 -> will create the role folder structure

* openssl rand -base64 2048 > vault.pass -> will create a strong random password and saves it into vault.pass.

* ansible-vault create group_vars/all/pass.yml --vault-password-file vault.pass -> this command creates a new encrypted YAML file (pass.yml) that you can safely store in Git or share, since it’s unreadable without the vault password.

* ansible-vault edit group_vars/all/pass.yml --vault-password-file vault.pass -> to edit the pass.yaml file use this command

* ansible-playbook -i Inventory/Hosts.ini ec2_create.yaml --vault-password-file vault.pass -> Execute the playbook with ansible vault


